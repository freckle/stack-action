{
  "devDependencies": {
    "action-docs": "github:npalm/action-docs",
    "prettier": "^3.2.4",
    "typescript": "^5.3.0"
  },
  "scripts": {
    "readme": "npx action-docs -u && prettier --write README.md"
  }
}
